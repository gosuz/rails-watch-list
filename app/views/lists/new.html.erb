<h1>This page is the page for the user to create a new list</h1>
<%# Allow users to create a new list %>
<%# Create a form %>
<%= simple_form_for(@list) do |f| %>
  <%= f.input :name %>
  <%= f.input :photo, as: :file %>
<%= f.submit %>
<% end %>
